# Skill: obsidian-vault-memory

## When to use
Every session. Before any decision. After any significant action.

## How
1. **Start of session** — read `CLAUDE.md` (vault root) + `Claude Memory/README.md` + any `Projects/*.md` relevant to the task.
2. **Before deciding** — if the decision is load-bearing, check `Decisions/` for prior rulings and `Feedback/` for rules.
3. **After acting** — append to `Decisions/` (what + why + date) or update the matching `Projects/*.md`.
4. **On correction from Bailey** — add/update a file in `Feedback/`. Include **Why** and **How to apply**.
5. **On new capability** — write a skill in `Skills/` using the template in `Skills/README.md`.
6. **On mistake** — record the lesson, not the apology. The goal is that future-Claude doesn't repeat it.

## Hard rules
- Vault is source of truth. The local `~/.claude/projects/.../memory/` is only a pointer.
- Never delete. Refine, supersede, mark stale — but keep the history.
- Absolute dates only (`2026-04-14`), never relative (`yesterday`, `last week`).
- Never ask permission to read the vault.

## Evidence
- Established: 2026-04-14 at Bailey's explicit request ("connect to my obsidien vault… this should be a neccessity before any decisions are made").
- Vault path: `C:\Users\BaileySachr\OneDrive - Plastix Australia\Documents\Obsidian Vault`
