# Skills — Claude's evolving toolkit

Drop a markdown file here for any reusable capability Claude should internalise. Format:

```
# Skill: <name>

## When to use
<trigger / task type>

## How
<steps, commands, gotchas>

## Evidence
<links to sessions, files, decisions where this worked or failed>
```

Read every file in this folder at the start of any task that touches the domain. Update/append after every use — evolve or retire the skill as reality teaches you.

## Seed list (to write as they mature)
- email-voice — drafting in Bailey's voice (see `../Feedback/email-voice.md`)
- outlook-web-automation — sending via Claude in Chrome
- github-issues-command-queue — dashboard ↔ backend plumbing
- railway-graphql-deploy — deploy/redeploy without CLI
- graph-webhook-setup — per-tenant Azure app + subscription renewal
- obsidian-vault-memory — this protocol itself
